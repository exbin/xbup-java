apply from: '../modules.gradle'

group = 'org.exbin.xbup.core'
def libraryName = 'XBUP Core Library'
def libraryDescription = 'Library for core XBUP support'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group : 'junit', name: 'junit', version: '4.10'
}
