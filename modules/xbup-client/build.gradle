group = 'org.exbin.xbup'
ext.libraryName = 'XBUP Service Client Library'
ext.libraryDescription = 'Library for client connection to XBUP service'

apply from: '../modules.gradle'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile(project(':modules:xbup-core'))
    compile "org.exbin.utils:exbin-binary_data:${exbinUtilsVersion}"
    testCompile group : 'junit', name: 'junit', version: '4.10'
}
