apply from: '../modules.gradle'

group = 'org.exbin.xbup.client'
def libraryName = 'XBUP Service Client Library'
def libraryDescription = 'Library for client connection to XBUP service'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenCentral()
}

dependencies {
    compile(project(':modules:xbup-core'))
    testCompile group : 'junit', name: 'junit', version: '4.10'
}
