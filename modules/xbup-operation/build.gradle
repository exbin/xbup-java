apply from: '../modules.gradle'

group = 'org.exbin.xbup.operation'
def libraryName = 'XBUP Operation Library'
def libraryDescription = 'Library for XBUP document editation'

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenCentral()
}

dependencies {
    compile(project(':modules:xbup-core'))
    compile(project(':modules:xbup-parser-tree'))
    testCompile group: 'junit', name: 'junit', version: '4.10'    
}
