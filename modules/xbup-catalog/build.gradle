group = 'org.exbin.xbup'
ext.libraryName = 'XBUP Audio Library'
ext.libraryDescription = 'Library for audio data types with XBUP support'

apply from: '../xbup-java-modules.gradle'

buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath("at.bxm.gradleplugins:gradle-svntools-plugin:latest.integration")
    }
}

if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile moduleDep('XbupJava', 'org.exbin.xbup', 'xbup-core')
    compile group: 'org.springframework', name: 'spring-core', version: '4.0.5.RELEASE'
    compile 'org.springframework:spring-core:4.0.5.RELEASE', 'org.springframework:spring-context:4.0.5.RELEASE', 'org.springframework:spring-beans:4.0.5.RELEASE'
    compile(
        [group: 'org.springframework', name: 'spring-core', version: '4.0.5.RELEASE'],
        [group: 'org.springframework', name: 'spring-context', version: '4.0.5.RELEASE'],
        [group: 'org.springframework', name: 'spring-beans', version: '4.0.5.RELEASE'],
    )
    compile 'javax.persistence:persistence-api:1.0.2'
    compile 'org.yaml:snakeyaml:1.13'
    compile group: 'javax.annotation', name: 'javax.annotation-api', version: '1.3.2'
    compile group: 'javax.activation', name: 'activation', version: '1.1.1'
    compile group: 'javax.jws', name: 'javax.jws-api', version: '1.1'
    compile moduleDep('ExbinAuxiliaryJava', 'org.exbin.auxiliary', 'paged_data')
    testCompile group: 'junit', name: 'junit', version: '4.10'    
}
